# Machine Configuration
MACHINE_ID="machine-001"
MACHINE_API_HOST="0.0.0.0"
MACHINE_API_PORT=8001
MACHINE_UDP_PORT=9999

# --- Motor Configuration ---
# Select the motor controller type. Options: simulation, dc, stepper, none.
# Defaults to "stepper" if not set.
# MOTOR_CONTROLLER="simulation"

# -- GPIO Pin Configuration for DC Motors (if using MOTOR_CONTROLLER="dc") --
# Uses BCM pin numbering.
# LEFT_MOTOR_FORWARD_PIN=17
# LEFT_MOTOR_BACKWARD_PIN=18
# RIGHT_MOTOR_FORWARD_PIN=27
# RIGHT_MOTOR_BACKWARD_PIN=22

# -- GPIO Pin Configuration for Stepper Motors (if using MOTOR_CONTROLLER="stepper") --
# Uses BCM pin numbering for STEP and DIR pins.
# LEFT_MOTOR_STEP_PIN=20
# LEFT_MOTOR_DIR_PIN=21
# RIGHT_MOTOR_STEP_PIN=23
# RIGHT_MOTOR_DIR_PIN=24

# -- GPIO Pin Configuration for Arm Servos --
# Uses BCM pin numbering.
# JOINT1_PIN=13
# JOINT2_PIN=19
# CLAMP_PIN=26


# --- Agent & Coordinator ---
# These are used by llm_agent.py and coordinator_client.py respectively.
# AGENT_TARGET_HOST="127.0.0.1"
# AGENT_TARGET_PORT=9999
# COORDINATOR_URL="http://127.0.0.1:8000"



# Motor Controller Configuration
# Options: "simulation", "gpio", "none"
MOTOR_CONTROLLER="simulation"

# --- GPIO Configuration (only used if MOTOR_CONTROLLER="gpio") ---
# Assign BCM pin numbers for the H-Bridge controller.
LEFT_MOTOR_FORWARD_PIN=23
LEFT_MOTOR_BACKWARD_PIN=24
RIGHT_MOTOR_FORWARD_PIN=27
RIGHT_MOTOR_BACKWARD_PIN=22
